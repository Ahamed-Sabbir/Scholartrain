spring:
  application:
    name : Scholartrain
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/Scholartrain
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: 12345
  expiry: 3600000